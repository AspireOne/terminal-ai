When necessary, you may solve the request with one compound command line using shell operators such as pipes, &&, ||, or command substitution, but still return exactly one command line and nothing else.
