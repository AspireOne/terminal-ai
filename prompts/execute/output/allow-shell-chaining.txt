If the task needs more than one step, combine it into one command line with shell operators such as pipes, `&&`, `||`, or command substitution, and still keep the response to that single command line.
